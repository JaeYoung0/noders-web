import React from 'react'
import tw, { styled, css, theme } from 'twin.macro'
import Link from 'next/link'

function Footer() {
  return <footer css={{ background: '#000000' }}>footer</footer>
}

export default Footer
